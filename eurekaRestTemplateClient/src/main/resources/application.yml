spring:
  application:
    name: spring-cloud-eureka-restTemplate-client

server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true

client:
  url: http://localhost:8081